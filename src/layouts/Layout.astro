---
import Navbar from "../components/Navbar.astro";
import cntl from "cntl";

interface Props {
  title: string;
  description: string;
  /// Draw the page behind a transparent navigation bar, as the landing hero does.
  overlayNav?: boolean;
}

const { title, description, overlayNav = false } = Astro.props;

const bodyClasses = cntl`
  bg-silver-tree-100
  dark:bg-silver-tree-800
  text-silver-tree-900
  dark:text-silver-tree-100
  min-h-full
`;

const footerClasses = cntl`
  text-center
  dark:bg-silver-tree-800
  bg-silver-tree-200
  text-silver-tree-600
  dark:text-silver-tree-200
  text-sm
  py-4
  border-t
  border-silver-tree-100
  dark:border-silver-tree-700
`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title}</title>
    <link rel="icon" href="/nahpu.svg" type="image/svg+xml" />
    <link rel="icon" href="/favicon.png" type="image/png" />
    <link rel="apple-touch-icon" href="/favicon.png" />
  </head>
  <body class={bodyClasses}>
    <Navbar overlay={overlayNav} />
    <main>
      <slot />
    </main>

    <footer class={footerClasses}>
      © {new Date().getFullYear()} the NAHPU project. All rights reserved.
    </footer>
  </body>
</html>

<style is:global>
  body {
    margin: 0;
    padding: 0;
    width: 100%;
    box-sizing: border-box;
  }
</style>
